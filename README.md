# I AM Loc Shop

Static website (red / business-card theme).

## Local preview

Open `index.html` or:

```bash
python -m http.server 8080
```

## Deploy

Upload this repository root (HTML, `css/`, `js/`, `assets/`) to any static host with HTTPS.
